<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to('Rails-Stripe', root_path, class: 'navbar-brand') %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Checkout.js", checkoutjs_charges_path %></li>
        <li><%= link_to "Custom Form", customform_charges_path %></li>
        <li><%= link_to "Remind credit card", following_purchase_charges_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= current_user.email %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to('<i class="glyphicon glyphicon-edit"></i> Edit Profile'.html_safe, edit_user_registration_path, class: 'dropmenu-item-btn') %></li>
                <li><%= link_to('<i class="glyphicon glyphicon-log-out"></i> Log Out  '.html_safe, destroy_user_session_path, method: 'delete', class: 'dropmenu-item-btn') %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to '<i class="glyphicon glyphicon-log-in"></i> Log In'.html_safe, new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>